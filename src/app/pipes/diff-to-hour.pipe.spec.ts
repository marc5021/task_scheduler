import { DiffToHourPipe } from './diff-to-hour.pipe';

describe('DiffToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new DiffToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
