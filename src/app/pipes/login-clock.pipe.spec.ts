import { LoginClockPipe } from './login-clock.pipe';

describe('LoginClockPipe', () => {
  it('create an instance', () => {
    const pipe = new LoginClockPipe();
    expect(pipe).toBeTruthy();
  });
});
