import { ArrayKeysPipe } from './array-keys.pipe';

describe('ArrayKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
